name: Sync Flows on VM

on:
  push:
    branches: [main]
    paths:
      - 'prefect_workflows/**'

jobs:
  sync:
    name: Pull latest code on VM
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v6
